version: "3.8"
services:
  db:
    image: postgres:13.4
    restart: always
    environment:
      POSTGRES_USER: kevin
      POSTGRES_PASSWORD: kevin
      POSTGRES_DB: school_database
    ports:
      - "5432:5432"
